[project]
name = "catchphrase-generator"
version = "0.1.0"
description = "Catchphrase Generator API using Amazon Bedrock Nova micro"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.35.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "moto[bedrock]>=5.0.0",
]
